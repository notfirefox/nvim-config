{
  "CMake Config": {
    "prefix": "cmake",
    "body": [
      "cmake_minimum_required(VERSION ${1:3.20})",
      "project(${2:project-name})\n",
      "set(CMAKE_EXPORT_COMPILE_COMMANDS ON)\n",
      "add_executable(main)",
      "target_sources(main PRIVATE ${0:<source files>})"
    ],
    "description": "Add CMake configuration"
  }
}
